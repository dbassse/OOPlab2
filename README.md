# 🛒 Система управления товарными чеками

Проект реализует систему для работы с товарными чеками, включая управление товарами и операциями с чеками. Код написан на Python с строгой типизацией и проходит проверку `mypy --strict`.

## 📦 Структура проекта

### 🎯 Класс `Goods` - Товар
Класс для представления товара с следующими характеристиками:

**Поля:**
- `code` (int) - уникальный код товара
- `name` (str) - наименование товара
- `price` (float) - цена за единицу
- `quantity` (int) - количество

**Основные методы:**
- `get_total()` - вычисление общей стоимости товара
- Геттеры/сеттеры для всех полей
- Конструктор инициализации строкой: `"код,наименование,цена,количество"`

### 🧾 Класс `Receipt` - Товарный чек
Класс для работы с товарными чеками:

**Характеристики:**
- Номер чека и дата создания
- Список товаров с ограничением максимального размера
- Поддержка операций индексирования

**Основные методы:**
- `add_goods()` - добавление товара
- `update_goods()` - обновление товара
- `remove_goods()` - удаление товара по коду
- `find_goods_by_code()` - поиск товара
- `get_total_sum()` - расчет общей суммы чека
- Перегрузка операторов `[]`, `len()`

## 🚀 Быстрый старт

### Установка и запуск

```bash
# Клонирование репозитория
git clone <repository-url>
cd receipt-system

# Создание виртуального окружения (опционально)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Запуск примера использования
python main.py
